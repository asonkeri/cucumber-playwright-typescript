# Cucumber, Playwright and TypeScript

## Requirements
NodeJS >= 14

## Setup
Install packages: `yarn`

## Run tests
Run `yarn test`

## Project structure
Cucumber feature files are placed in `features` folder and step definitions and utilities are found under `src`.

